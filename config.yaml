port: 8080
jwt_secret: your-secret-key

routes:
  - path: /users
    auth: false
    backends:
      - http://localhost:3001

  - path: /orders
    auth: true
    backends:
      - http://localhost:3002
      - http://localhost:3003